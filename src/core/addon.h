#ifndef NODE_CPP_NPM_PACKAGE_BOILERPLATE_ADDON
#define NODE_CPP_NPM_PACKAGE_BOILERPLATE_ADDON

#include <nan.h>
#include "hello.h"

#define LOG(msg) ;
#define TRACEF(format, arg) ;

#define TRACE(msg) LOG(msg);

#endif
